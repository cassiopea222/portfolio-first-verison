import { IconArrowLeft } from "@tabler/icons-react";

/** Tabler “arrow left” for decorative back / previous links; inherits `currentColor`. */
export function ArrowBackNavIcon() {
  return <IconArrowLeft aria-hidden className="shrink-0" size={20} stroke={1.75} />;
}
